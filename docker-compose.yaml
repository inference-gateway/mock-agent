# Code generated by ADL CLI v0.48.3. DO NOT EDIT.
# This file was automatically generated from an ADL (Agent Definition Language) specification.
# Manual changes to this file may be overwritten during regeneration.

# Local development stack for mock-agent.
#
# Brings up the Inference Gateway, this A2A agent (built from the local
# Dockerfile so code changes are picked up), and gives you opt-in profiles
# for the `infer` CLI and the `a2a-debugger` so the agent can be exercised
# end-to-end without manual wiring.
#
#   docker compose up --build                             # gateway + agent
#   docker compose --profile cli run --rm cli chat        # interactive infer CLI
#   docker compose --profile debugger run --rm debugger --server-url http://mock-agent:8080 tasks submit-streaming "What are your skills?"
---
services:
  gateway:
    image: ghcr.io/inference-gateway/inference-gateway:latest
    pull_policy: always
    restart: unless-stopped
    env_file:
      - path: .env
        required: false
    environment:
      ENVIRONMENT: ${ENVIRONMENT:-production}
      SERVER_HOST: 0.0.0.0
      SERVER_PORT: "8080"

  mock-agent:
    build: .
    restart: unless-stopped
    env_file:
      - path: .env
        required: false
    environment:
      A2A_SERVER_PORT: "8080"
      A2A_DEBUG: "${A2A_DEBUG:-false}"
      A2A_AGENT_CLIENT_PROVIDER: "${A2A_AGENT_CLIENT_PROVIDER:-}"
      A2A_AGENT_CLIENT_MODEL: "${A2A_AGENT_CLIENT_MODEL:-}"
      A2A_AGENT_CLIENT_BASE_URL: "${A2A_AGENT_CLIENT_BASE_URL:-http://gateway:8080/v1}"
      A2A_ARTIFACTS_ENABLE: "true"
      A2A_ARTIFACTS_SERVER_HOST: mock-agent
      A2A_ARTIFACTS_SERVER_PORT: "8081"
      A2A_ARTIFACTS_STORAGE_PROVIDER: filesystem
      A2A_ARTIFACTS_STORAGE_BASE_PATH: /tmp/artifacts
      A2A_ARTIFACTS_RETENTION_MAX_ARTIFACTS: "10"
      A2A_ARTIFACTS_RETENTION_MAX_AGE: 24h
      A2A_ARTIFACTS_RETENTION_CLEANUP_INTERVAL: 24h
    depends_on:
      gateway:
        condition: service_started

  cli:
    image: ghcr.io/inference-gateway/cli:latest
    profiles:
      - cli
    pull_policy: always
    stdin_open: true
    tty: true
    env_file:
      - path: .env
        required: false
    environment:
      INFER_GATEWAY_URL: "http://gateway:8080"
      INFER_A2A_ENABLED: "true"
      INFER_TOOLS_ENABLED: "true"
      INFER_A2A_TOOLS_QUERY_AGENT_ENABLED: "true"
      INFER_A2A_TOOLS_SUBMIT_TASK_ENABLED: "true"
      INFER_A2A_TOOLS_QUERY_TASK_ENABLED: "true"
      INFER_TOOLS_BASH_ENABLED: "false"
      INFER_TOOLS_TODO_WRITE_ENABLED: "false"
      INFER_TOOLS_WRITE_ENABLED: "false"
      INFER_TOOLS_READ_ENABLED: "false"
      INFER_TOOLS_DELETE_ENABLED: "false"
      INFER_TOOLS_EDIT_ENABLED: "false"
      INFER_TOOLS_GREP_ENABLED: "false"
      INFER_TOOLS_TREE_ENABLED: "false"
      INFER_TOOLS_WEB_FETCH_ENABLED: "true"
      INFER_TOOLS_WEB_SEARCH_ENABLED: "false"
      INFER_TOOLS_GITHUB_ENABLED: "false"
      INFER_TOOLS_WEB_FETCH_WHITELISTED_DOMAINS: |
        - mock-agent
      INFER_AGENT_MODEL: ${CLI_PROVIDER:-}/${CLI_MODEL:-}
      INFER_A2A_AGENTS: |
        http://mock-agent:8080
    volumes:
      - ./tmp:/home/infer/.infer/tmp
    depends_on:
      gateway:
        condition: service_started

  debugger:
    image: ghcr.io/inference-gateway/a2a-debugger:latest
    profiles:
      - debugger
    pull_policy: always
    stdin_open: true
    tty: true
    depends_on:
      mock-agent:
        condition: service_started
